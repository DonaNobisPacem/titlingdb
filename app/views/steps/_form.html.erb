<%= simple_form_for(@step) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :date_start %>
    <%= f.input :description %>
    <%= f.association :title %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
